---
const { title, subtitle } = Astro.props;
---

<div class="flex flex-col gap-2">
  <h2 class="text-xs font-semibold tracking-[0.3em] text-gold-500 uppercase">
    {title}
  </h2>
  {subtitle && <p class="text-sm text-slate-400/80">{subtitle}</p>}
</div>
